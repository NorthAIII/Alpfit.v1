# Expo + Metro pnpm uyumu için mobile workspace flat node_modules kullanır.
# Metro hierarchical lookup ve pnpm'in .pnpm symlink yapısı çatışıyor; transitive
# native module'ler (expo-modules-core, react-native vb.) flat layout gerektiriyor.
# Sadece mobile/'a uygulanır — backend/shared pnpm default layout'ta kalır.
# Detay: PHASE-1 §Tuzaklar #1 + TASK-1.05 Risk Planı.
node-linker=hoisted
